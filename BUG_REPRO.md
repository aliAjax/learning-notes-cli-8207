# Bug 复现说明

## Bug 是什么
标签的解析、去重、排序和搜索匹配逻辑不一致，导致标签列表出现重复/乱序，且 `search --tag` 无法按大小写无关的完整标签匹配。

## 如何触发
1. 在数据目录中创建带标签的笔记，例如 `new --title "note" --tags " Go;cli, go;CLI " --content "body"`。
2. 运行 `search --tag GO` 或读取已保存笔记，观察标签结果。

## 错误信息
标签输出为 `Go;cli`、`go;CLI` 或原始重复项；大小写不同的标签搜索返回空结果。
